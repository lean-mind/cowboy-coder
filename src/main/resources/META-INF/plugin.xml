<idea-plugin>
    <id>es.leanmind.unique.cowboyCoder.1</id>
    <name>CowboyCoder</name>
    <version>0.1</version>
    <vendor email="contact@leanmind.es" url="http://leanmind.es">Lean Mind</vendor>

    <!-- Description will be read from patchPluginXml task's attribute "pluginDescription" (see build.gradle) -->
    <!-- <description></description> -->

    <!-- please see https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html for description -->
    <!-- Lowest compatible version (since-build) will be read from "patchPluginXml" task's attribute "sinceBuild" (see build.gradle) -->
    <!-- Highest compatible version (until-build) will be inferred from "intellij" task's attribute "version" (see build.gradle) -->
    <!--<idea-version since-build="223.0" until-build="223.*"/>-->

    <!-- please see https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <typedHandler implementation="CowboyCoderHandler"/>
    </extensions>

    <actions>
        <!-- Add your actions here -->
    </actions>
</idea-plugin>
